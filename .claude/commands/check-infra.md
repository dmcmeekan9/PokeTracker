Run `bash scripts/validate-checkout.sh` from the project root.

Report only:
- **Pass**: show the timing line from the script output
- **Fail**: show the exact error message from the script and suggest which component to check (EC2, Lambda, or Target session)

Do not read any project files, explore the codebase, or explain what the script does. The script is self-contained and takes ~90 seconds.
